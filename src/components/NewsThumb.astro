---
// TEMPORARY: plain placeholder card art while final on-brand news imagery is
// produced. Keeps the news grid uniform for stakeholder review. The previous
// generated mini-scene version lives in git history (NewsThumb pre-2026-07-19)
// if we want to revive parts of it.
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'news'>;
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['img-frame', className]}>
  <div class="ph" role="img" aria-label="Placeholder — article image coming soon"></div>
</div>
